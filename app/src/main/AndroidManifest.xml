<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.zweng.xposed.smartcardreaderfix">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Only tested on LeEco LePro 3 (LEX720) device! Changes the behaviour of Open Mobile API (OMAPI) SmartcardService so that it works with Austrian BankCard mobil apps. See github.com/johnzweng/XposedOmapiBankcardFix for details." />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />

    </application>

</manifest>
